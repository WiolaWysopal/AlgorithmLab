apiVersion: v1
kind: ConfigMap
metadata:
  name: algorithmlab-config
  namespace: algorithmlab

data:
  DB_HOST: postgres-service
  DB_PORT: "5432"
  DB_USER: algorithmlab
  DB_PASSWORD: algorithmlab
  DB_NAME: algorithmlab
  PORT: "5000"
